name: Test
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker run -ti -v $PWD/src:/usr/src/myapp -v $PWD/bin:/usr/src/bin openjdk:11 /usr/src/bin/run